import 'package:flutter/material.dart';

class ConfirmPaymentAndOrder extends StatelessWidget {
  const ConfirmPaymentAndOrder({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: [
            Image.asset("images/")
          ],
        ),
      ),
    );
  }
}
